CREATE DATABASE cgm
    WITH 
    OWNER = jdegruson
    ENCODING = 'UTF8'
    CONNECTION LIMIT = -1;
	
	
CREATE TABLE patient
(
    id SERIAL PRIMARY KEY,
    name varchar (30),
	surname varchar (30),
	birth_date DATE,
	social_security_number varchar (255)
);

ALTER TABLE patient
    OWNER to jdegruson;
	
	
insert into patient (name,surname,birth_date,social_security_number) values ('testn','testsn','1996-12-02','testssn');